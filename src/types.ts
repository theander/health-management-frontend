export type UserType = {
  username: string;
  password: string;
  access_token: string;
  refresh_token: string;
};
export type CardPropsType = {
  title: string;
  description: string;
  redirectTo: string;
  redirectDescription: string;
};
